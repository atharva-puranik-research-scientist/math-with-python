# Program to demonstrate creating graphs with  matplotlib in python with IDLE
# chapter 02 pg-no.34 -- math with python
# programmer - Atharva Puranik

from pylab import plot, show
x_numbers = [1, 2, 3]
y_numbers = [2, 4, 6]
plot(x_numbers, y_numbers, marker = 'o')
show()
